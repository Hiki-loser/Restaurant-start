<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CategoryMapper">

    <insert id="insert" parameterType="com.sky.entity.Category">
        insert into category (type, name, sort, status, create_time, update_time, create_user, update_user)
        values (#{type}, #{name}, #{sort}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <update id="update" parameterType="com.sky.entity.Category">
        update category
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="name != null">name = #{name},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 分页查询：根据 name 模糊匹配，可扩展其他筛选条件 -->
    <select id="pageQuery" parameterType="com.sky.dto.CategoryPageQueryDTO" resultType="com.sky.entity.Category">
        SELECT
            id,
            type,
            name,
            sort,
            status,
            create_time AS createTime,
            update_time AS updateTime,
            create_user AS createUser,
            update_user AS updateUser
        FROM category
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="type != null">
                AND type = #{type}
            </if>
        </where>
        ORDER BY sort ASC, update_time DESC
    </select>

    <!-- 列表查询：根据 type 和 status 筛选 -->
    <select id="categoryList" resultType="com.sky.entity.Category">
        SELECT
            id,
            type,
            name,
            sort,
            status,
            create_time AS createTime,
            update_time AS updateTime,
            create_user AS createUser,
            update_user AS updateUser
        FROM category
        <where>
            <if test="type != null">
                type = #{type}
            </if>
                AND status = 1
        </where>
        ORDER BY sort ASC, update_time DESC
    </select>

</mapper>

